import { Box, Typography } from "@mui/material";
import React from "react";

export const Manager: React.FC = () => {
    return (
        <Box>
            <Typography variant="h4">Manager Page</Typography>
            <Typography>Bem-vindo à página de manager!</Typography>
        </Box>
    );
};